# 笔记功能增强与Bug修复 - 任务拆解

> 基于需求文档：`docs/requirements/需求分析_滑动误触与笔记功能增强.md`
> 任务按优先级和依赖关系排序

---

## 📌 2026-03-06：11 项需求（1-11）落地同步

> 用于记录近期已落地的跨文件改动（避免打乱原 Phase 结构）

- ✅ **撤销/重做快捷键**：Ctrl/Cmd + Z / Ctrl+Y（含 Cmd+Shift+Z）
- ✅ **粘贴优化（不含图片粘贴）**：HTML table / TSV → Markdown 表格 + 富文本清理
- ✅ **移动端输入稳定性**：组合态期间避免 `setSelectionRange` 打断，减少光标跳动
- ✅ **新建按钮 UI**：文件夹内新建下拉；根目录新建主按钮（默认新建文件夹）
- ✅ **移动端拇指可及**：列表页 FAB；编辑页底部保存入口 safe-area 适配
- ✅ **触觉反馈**：新增 `haptics` 并接入关键操作
- ✅ **表格增强**：列宽拖拽 + 首列冻结（需执行 `docs/sql/create_note_table_layouts.sql`）
- ✅ **最近打开**：Dashboard 最近打开（默认折叠，折叠状态本地持久化）
- ✅ **长文优化**：预览态块级虚拟化 + 编辑态段级虚拟化
- ✅ **离线首屏**：缓存优先展示、网络回填（列表/内容）
- ✅ **启动优化**：Dashboard 路由/最近笔记页预取

相关文件（节选）：
- `src/components/NoteManager.tsx`、`src/components/NoteEditor.tsx`、`src/components/SegmentedEditor.tsx`
- `src/components/MarkdownRenderer.tsx`、`src/app/dashboard/page.tsx`
- `src/lib/offline-storage.ts`、`src/lib/recent-notes.ts`、`src/lib/haptics.ts`
- `docs/sql/create_note_table_layouts.sql`

## 📋 Phase 0: Bug修复（P0 - 必须）

### Task 0.1: 修复滑动误触问题
**优先级**: 🔴 P0（必须）  
**预估时间**: 2-3 小时  
**依赖**: 无  
**状态**: ✅ 已完成

**任务内容**:
- 修改 `src/components/MindNoteManager.tsx`
- 在 `handleTouchStart` 中记录初始触摸位置（`touchstart` 事件的坐标）
- 添加 `handleTouchMove` 处理函数，检测滑动距离
- 计算触摸移动距离：`Math.sqrt((x2-x1)^2 + (y2-y1)^2)`
- 如果移动距离超过阈值（10-15px），清除长按定时器
- 在 `DraggableFolderCard` 和 `DraggableMindNoteCard` 组件上添加 `onTouchMove` 事件处理
- 确保滑动操作不会触发选择模式

**技术实现要点**:
- 使用 `useRef` 存储初始触摸位置和定时器引用
- 在 `touchmove` 事件中实时检测滑动距离
- 如果检测到滑动，立即调用 `clearTimeout` 清除长按定时器
- 确保长按功能仍然正常工作（500ms延迟）

**验收标准**:
- [ ] 在移动端滑动页面时，不会误触发笔记选中
- [ ] 长按功能仍然正常工作（500ms延迟）
- [ ] PC端点击和拖拽功能不受影响
- [ ] 移动端滑动体验流畅，无误触

**相关文件**:
- `src/components/MindNoteManager.tsx`
- `src/components/DraggableFolderCard.tsx`（如果存在）
- `src/components/DraggableMindNoteCard.tsx`（如果存在）

---

### Task 0.2: 修复任务详情拖拽冲突
**优先级**: 🔴 P0（必须）  
**预估时间**: 0.5 小时  
**依赖**: 无  
**状态**: ✅ 已完成

**任务内容**:
- 修改 `src/components/TodoDetail.tsx`
- 在弹窗最外层容器上添加事件处理器，阻止拖拽事件传播
- 阻止 `mousedown`、`touchstart`、`dragstart` 事件冒泡到父级

**技术实现要点**:
- 在 `TodoDetail` 的最外层容器（遮罩层和内容容器）上添加事件处理器
- 使用 `e.stopPropagation()` 阻止事件冒泡到父级的 `DndContext`
- 确保弹窗内部的正常交互不受影响

**验收标准**:
- [x] 在任务详情页面选中文字时，不会触发任务拖动
- [x] 任务详情弹窗内部的交互正常（输入、选择等）
- [x] 任务列表的拖拽功能仍然正常工作

**相关文件**:
- `src/components/TodoDetail.tsx`

---

## 📋 Phase 1: 笔记统计信息（P1 - 高优先级）

### Task 1.1: 创建笔记统计信息组件
**优先级**: 🟢 P1（高优先级）  
**预估时间**: 1-2 小时  
**依赖**: 无  
**状态**: ✅ 已完成

**任务内容**:
- 创建 `src/components/NoteStats.tsx` 组件
- 实现字数统计功能：
  - 统计 `content` 的字符数（中文字符按1个计算）
  - 使用 `useMemo` 优化计算，避免频繁重新计算
  - 实时更新（在 `content` 变化时重新计算）
- 实现时间显示功能：
  - 格式化 `created_at` 和 `updated_at`
  - 使用友好的时间格式（如"2024年1月20日 14:30"）
  - 支持相对时间显示（可选，如"2小时前"）
- UI设计：
  - 固定在编辑器底部
  - 使用较小的字体和 muted 颜色
  - 响应式设计，移动端适配

**技术实现要点**:
- 使用 `useMemo` 计算字数：`content.length`
- 创建日期格式化函数或使用 `date-fns` 库
- 组件接收 `content`、`created_at`、`updated_at` 作为 props

**验收标准**:
- [ ] 显示字数统计（实时更新）
- [ ] 显示创建时间（友好格式）
- [ ] 显示最后编辑时间（友好格式）
- [ ] 在编辑器底部固定显示
- [ ] 移动端布局正常

**相关文件**:
- `src/components/NoteStats.tsx`（新建）
- `src/components/NoteManager.tsx`（集成）

---

### Task 1.2: 集成统计信息到笔记编辑器
**优先级**: 🟢 P1（高优先级）  
**预估时间**: 0.5-1 小时  
**依赖**: Task 1.1  
**状态**: ✅ 已完成

**任务内容**:
- 修改 `src/components/NoteManager.tsx`
- 在编辑器底部集成 `NoteStats` 组件
- 传递必要的 props：`content`、`created_at`、`updated_at`
- 确保统计信息在编辑模式和预览模式下都显示
- 调整编辑器布局，为统计信息预留空间

**技术实现要点**:
- 在编辑器容器底部添加 `NoteStats` 组件
- 确保统计信息不遮挡编辑器内容
- 使用 `sticky` 或 `fixed` 定位（可选）

**验收标准**:
- [ ] 统计信息正确显示在编辑器底部
- [ ] 统计信息实时更新
- [ ] 不影响编辑器正常使用
- [ ] 移动端显示正常

**相关文件**:
- `src/components/NoteManager.tsx`

---

## 📋 Phase 2: 笔记查找与替换（P2 - 中优先级）

### Task 2.1: 创建查找替换工具函数
**优先级**: 🟡 P2（中优先级）  
**预估时间**: 2-3 小时  
**依赖**: 无  
**状态**: ✅ 已完成

**任务内容**:
- 创建 `src/lib/search-utils.ts`
- 实现查找相关工具函数：
  - `findAllMatches(text, query, caseSensitive?)` - 查找所有匹配项，返回位置数组
  - `getMatchIndex(matches, cursorPosition)` - 根据光标位置获取当前匹配项索引
  - `getNextMatchIndex(currentIndex, matches)` - 获取下一个匹配项索引
  - `getPreviousMatchIndex(currentIndex, matches)` - 获取上一个匹配项索引
  - `highlightMatches(text, query, caseSensitive?)` - 高亮匹配项（返回带标记的文本）
- 实现替换相关工具函数：
  - `replaceMatch(text, query, replacement, matchIndex)` - 替换单个匹配项
  - `replaceAllMatches(text, query, replacement, caseSensitive?)` - 替换所有匹配项

**技术实现要点**:
- 使用正则表达式进行文本匹配
- 支持大小写敏感选项
- 处理特殊字符转义
- 返回匹配项的位置信息（start, end）

**验收标准**:
- [ ] 所有工具函数实现完成
- [ ] 类型定义完整（TypeScript）
- [ ] 支持大小写敏感选项
- [ ] 正确处理特殊字符
- [ ] 单元测试通过（可选）

**相关文件**:
- `src/lib/search-utils.ts`（新建）

---

### Task 2.2: 创建查找替换对话框组件
**优先级**: 🟡 P2（中优先级）  
**预估时间**: 3-4 小时  
**依赖**: Task 2.1  
**状态**: ✅ 已完成

**任务内容**:
- 创建 `src/components/FindReplaceDialog.tsx` 组件
- 实现查找功能：
  - 查找输入框
  - 实时高亮匹配项
  - 显示匹配项数量（如 "1/5"）
  - "上一个"/"下一个"按钮
  - 支持大小写敏感选项（可选）
- 实现替换功能：
  - 替换输入框
  - "替换"按钮（单个）
  - "全部替换"按钮
  - 替换后自动定位到下一个匹配项
- UI设计：
  - 固定在编辑器顶部或底部
  - 支持关闭按钮（`Esc` 键）
  - 移动端适配

**技术实现要点**:
- 使用 `Dialog` 或 `Popover` 组件（或自定义固定面板）
- 监听键盘事件（`Enter`、`Shift+Enter`、`Esc`）
- 使用 `Textarea` 的 `selectionStart` 和 `selectionEnd` 来定位文本
- 实现文本高亮（可以使用 `mark` 标签或自定义样式）

**验收标准**:
- [ ] 查找输入框正常工作
- [ ] 实时高亮所有匹配项
- [ ] 显示匹配项数量
- [ ] 支持上下切换匹配项
- [ ] 支持单个替换和全部替换
- [ ] 支持 `Esc` 键关闭
- [ ] 移动端显示正常

**相关文件**:
- `src/components/FindReplaceDialog.tsx`（新建）
- `src/components/ui/dialog.tsx`（如果使用）

---

### Task 2.3: 集成查找替换到笔记编辑器
**优先级**: 🟡 P2（中优先级）  
**预估时间**: 2-3 小时  
**依赖**: Task 2.2  
**状态**: ✅ 已完成

**任务内容**:
- 修改 `src/components/NoteManager.tsx`
- 添加查找替换状态管理：
  - `isFindReplaceOpen` - 控制查找替换面板显示/隐藏
  - `findQuery` - 查找关键词
  - `replaceQuery` - 替换文本
  - `currentMatchIndex` - 当前匹配项索引
  - `matches` - 所有匹配项数组
- 实现快捷键：
  - `Ctrl+F`（PC）打开查找面板
  - `Ctrl+H`（PC）打开替换面板
  - `Esc` 关闭面板
- 集成 `FindReplaceDialog` 组件
- 实现查找替换逻辑：
  - 调用 `search-utils` 中的工具函数
  - 更新编辑器内容（替换时）
  - 定位到匹配项位置
  - 高亮显示匹配项

**技术实现要点**:
- 使用 `useEffect` 监听键盘快捷键
- 在编辑模式下显示查找替换面板
- 替换时更新 `content` 状态
- 使用 `textareaRef.current.setSelectionRange()` 定位光标

**验收标准**:
- [ ] 支持 `Ctrl+F` 快捷键打开查找面板
- [ ] 支持 `Ctrl+H` 快捷键打开替换面板
- [ ] 查找替换功能正常工作
- [ ] 替换后内容正确更新
- [ ] 移动端通过工具栏按钮触发（可选）

**相关文件**:
- `src/components/NoteManager.tsx`

---

## 📋 Phase 3: 全局查找增强（P3 - 中优先级）

### Task 3.1: 增强搜索结果列表导航
**优先级**: 🟡 P3（中优先级）  
**预估时间**: 2-3 小时  
**依赖**: Task 2.1（复用查找工具函数）  
**状态**: ✅ 已完成

**任务内容**:
- 修改 `src/app/dashboard/page.tsx`
- 在搜索结果列表中添加键盘导航：
  - 上下箭头键切换选中结果
  - `Enter` 键打开选中的结果
- 实现结果高亮：
  - 当前选中的结果高亮显示
  - 鼠标悬停时高亮
- 添加状态管理：
  - `selectedResultIndex` - 当前选中的结果索引
  - 监听键盘事件（上下箭头键）

**技术实现要点**:
- 使用 `useState` 管理选中索引
- 使用 `useEffect` 监听键盘事件
- 使用 `scrollIntoView` 确保选中结果可见
- 高亮显示当前选中的结果

**验收标准**:
- [ ] 在搜索结果中可以使用上下箭头键切换
- [ ] 当前选中的结果高亮显示
- [ ] `Enter` 键可以打开选中的结果
- [ ] 鼠标点击可以切换选中结果

**相关文件**:
- `src/app/dashboard/page.tsx`

---

### Task 3.2: 实现笔记详情页内结果切换
**优先级**: 🟡 P3（中优先级）  
**预估时间**: 3-4 小时  
**依赖**: Task 2.1（复用查找工具函数）  
**状态**: ✅ 已完成

**任务内容**:
- 修改 `src/app/notes/[id]/page.tsx`
- 解析所有匹配项的位置（使用 `search-utils`）
- 实现"下一个"/"上一个"按钮：
  - 显示当前匹配项位置（如 "2/5"）
  - 支持快捷键（如 `Ctrl+G` / `F3`）
- 实现匹配项切换逻辑：
  - 记录当前匹配项索引
  - 切换时滚动到对应位置
  - 高亮显示当前匹配项
- UI设计：
  - 浮动按钮或工具栏
  - 显示匹配项位置信息

**技术实现要点**:
- 使用 `search` query参数获取搜索关键词（已实现）
- 使用 `TreeWalker` 或 `querySelectorAll` 查找所有文本节点
- 使用 `scrollIntoView` 滚动到匹配项位置
- 高亮显示当前匹配项（使用 `mark` 标签）

**验收标准**:
- [ ] 在笔记详情页中，如果通过搜索进入，可以切换页面内的匹配项
- [ ] 显示当前匹配项位置（如 "2/5"）
- [ ] 切换时自动滚动到匹配项位置并高亮
- [ ] 支持快捷键切换（可选）

**相关文件**:
- `src/app/notes/[id]/page.tsx`

---

## 📋 Phase 4: 表格支持（P4 - 低优先级）

### Task 4.1: 实现基础表格插入功能
**优先级**: 🔵 P4（低优先级）  
**预估时间**: 2-3 小时  
**依赖**: 无  
**状态**: ✅ 已完成

**任务内容**:
- 修改 `src/components/NoteManager.tsx`
- 添加"插入表格"工具栏按钮
- 实现表格插入功能：
  - 插入标准 Markdown 表格语法（2x2）
  - 光标定位到第一个单元格
  - 表格模板：
    ```
    |  |  |
    |--|--|
    |  |  |
    ```
- 使用 `Textarea` 的 `setSelectionRange` 定位光标

**技术实现要点**:
- 获取当前光标位置
- 插入表格 Markdown 语法
- 计算插入后光标应该定位的位置（第一个单元格）
- 更新 `content` 状态

**验收标准**:
- [ ] 支持插入2x2表格（Markdown格式）
- [ ] 插入后光标定位到第一个单元格
- [ ] 表格在预览模式下正确渲染
- [ ] 工具栏按钮正常显示

**相关文件**:
- `src/components/NoteManager.tsx`

---

### Task 4.2: 实现表格编辑辅助工具（可选）
**优先级**: 🔵 P4（低优先级）  
**预估时间**: 3-4 小时  
**依赖**: Task 4.1  
**状态**: ✅ 已完成

**任务内容**:
- 创建表格编辑辅助工具：
  - 快速添加行（在表格末尾添加一行）
  - 快速添加列（在表格末尾添加一列）
  - 表格格式化（对齐表格列）
- 实现表格解析功能：
  - 解析 Markdown 表格语法
  - 检测当前光标是否在表格内
  - 获取表格的行数和列数

**技术实现要点**:
- 使用正则表达式解析 Markdown 表格
- 检测光标位置是否在表格内
- 插入新行/列时保持表格格式

**验收标准**:
- [ ] 可以快速添加表格行
- [ ] 可以快速添加表格列
- [ ] 表格格式化功能正常
- [ ] 检测光标位置功能正常

**相关文件**:
- `src/lib/table-utils.ts`（新建，可选）
- `src/components/NoteManager.tsx`

---

### Task 4.3: 实现可视化表格编辑器（可选，高级功能）
**优先级**: 🔵 P4（低优先级）  
**预估时间**: 8-10 小时  
**依赖**: Task 4.1, Task 4.2  
**状态**: ✅ 已完成

**任务内容**:
- 创建 `src/components/TableEditor.tsx` 组件
- 实现可视化表格编辑：
  - 在编辑模式下，表格显示为可编辑的HTML表格
  - 支持添加/删除行/列
  - 支持单元格编辑
  - 编辑完成后，将HTML表格转换回Markdown格式
- 实现表格与Markdown的双向转换：
  - Markdown → HTML表格
  - HTML表格 → Markdown

**技术实现要点**:
- 使用 `contentEditable` 或自定义表格组件
- 实现表格与Markdown的双向转换
- 检测表格边界，支持添加/删除行/列

**验收标准**:
- [ ] 表格在编辑模式下显示为可编辑的HTML表格
- [ ] 支持添加/删除行/列
- [ ] 支持单元格编辑
- [ ] 表格与Markdown转换正确

**相关文件**:
- `src/components/TableEditor.tsx`（新建，可选）
- `src/lib/table-utils.ts`

---

## 📋 Phase 5: 笔记导出功能（P2 - 中优先级）

### Task 5.1: 实现笔记导出功能
**优先级**: 🟡 P2（中优先级）  
**预估时间**: 4-6 小时  
**依赖**: 无  
**状态**: ✅ 已完成

**任务内容**:
- 创建 `src/components/ExportDialog.tsx` 组件
- 创建 `src/components/ui/checkbox.tsx` 组件（支持 indeterminate 状态）
- 在 `NoteManager` 中添加导出按钮
- 实现导出选择对话框：
  - 显示所有文件夹和笔记（树形结构）
  - 默认展开所有文件夹
  - 支持文件夹展开/折叠
  - 全选功能
  - 文件夹选择（支持全选/半选/未选状态）
  - 单个笔记选择
- 实现导出逻辑：
  - 使用 `jszip` 创建 ZIP 压缩包
  - 按文件夹组织笔记
  - 未分类笔记放在根目录
  - 文件命名处理特殊字符

**技术实现要点**:
- 使用 `Dialog` 组件创建导出对话框
- 使用 `Checkbox` 组件实现选择功能
- 使用 `Set` 管理选中状态
- 使用 `jszip` 和 `file-saver` 实现导出

**验收标准**:
- [x] 导出对话框正确显示
- [x] 文件夹和笔记列表正确展示
- [x] 全选功能正常工作
- [x] 文件夹选择功能正常（全选/半选/未选）
- [x] 单个笔记选择功能正常
- [x] 导出为 ZIP 压缩包成功
- [x] 压缩包内文件结构正确
- [x] 移动端和桌面端都正常

**相关文件**:
- `src/components/ExportDialog.tsx`（新建）
- `src/components/ui/checkbox.tsx`（新建）
- `src/components/NoteManager.tsx`（修改）

---

## 📋 Phase 6: 移动端编辑体验优化（P0 - 高优先级）

> 参考 PRD：`docs/productmanager/移动端编辑体验优化_PRD.md`

### Task 6.1: 定义并实现移动端写作模式（Keyboard Writing Mode）
**优先级**: 🔴 P0（必须）  
**预估时间**: 4-6 小时  
**依赖**: 现有 NoteManager / SegmentedEditor 实现  
**状态**: ⏳ 待开始

**任务内容**:
- 在 `NoteManager` 中识别“移动端 + 键盘弹出 + 正文聚焦”的写作状态：
  - 通过 UA + 视口高度变化，或聚焦事件 + UA 来判断移动端键盘态。
- 在写作模式下调整编辑页布局：
  - 隐藏标签编辑区和笔记统计信息组件。
  - 收窄标题区域的上下 padding，高度压缩为单行展示（若标题已填写）。
  - 确保键盘上方 ≥ 70% 像素高度用于显示正文内容。
- 提供退出写作模式的逻辑：
  - 关闭键盘 / 点击非输入区域 / 返回按钮时，恢复正常布局。

**技术实现要点**:
- 在 `NoteManager` 中新增写作模式状态（如 `isMobileWritingMode`）。
- 通过 `window.innerHeight` 变化 + focus/blur 事件组合判断键盘弹出与收起。
- 使用条件渲染（或类名控制）隐藏/显示相关区域，确保桌面端逻辑不受影响。

**验收标准**:
- [ ] 移动端键盘弹出且聚焦正文时，自动进入写作模式。
- [ ] 写作模式下标签区和统计信息不再占用竖向空间。
- [ ] 键盘上方可视区域中，正文内容区域 ≥ 70% 像素高度。
- [ ] 收起键盘或点击返回按钮后，布局恢复到非写作模式。

**相关文件**:
- `src/components/NoteManager.tsx`
- `docs/productmanager/移动端编辑体验优化_PRD.md`

---

### Task 6.2: 重构移动端底部工具栏为双形态（浏览态 & 写作态）
**优先级**: 🔴 P0（必须）  
**预估时间**: 4-6 小时  
**依赖**: Task 6.1（写作模式状态）  
**状态**: ⏳ 待开始

**任务内容**:
- 在移动端区分两种状态下的工具栏形态：
  - 浏览态（未弹出键盘）：保留现有底部工具栏，但收敛为少量主按钮（如保存 / 预览 / 更多）。
  - 写作态（键盘弹出）：隐藏整条底部横向工具栏，仅保留：
    - 右下角浮动主按钮（如“完成”或“更多”）；或
    - 一条窄“手柄条”，上滑拉出完整操作面板。
- 通过“更多（⋯）”按钮提供额外操作入口，不再将所有操作常驻在屏幕底部。

**技术实现要点**:
- 在 `NoteManager` 的移动端底部工具栏处根据 `isMobileWritingMode` 条件渲染不同的 UI。
- 写作态下使用 `fixed` + 圆形按钮或极窄条，避免与键盘重叠占用过多空间。
- 确保浮动按钮不会挡住正文主要内容（位置和透明度需要微调）。

**验收标准**:
- [ ] 浏览态下仍有保存 / 预览等主操作入口，操作路径不增加超过 1 次点击。
- [ ] 写作态下不再出现整条底部大工具栏，只保留轻量入口。
- [ ] 切换态时有简单的淡入/淡出或滑动动画，不影响输入焦点。
- [ ] 桌面端工具栏样式和行为完全不受影响。

**相关文件**:
- `src/components/NoteManager.tsx`
- `docs/productmanager/移动端编辑体验优化_PRD.md`

---

### Task 6.3: 简化移动端滚动恢复逻辑，消除“编辑跳到顶部”问题
**优先级**: 🔴 P0（必须）  
**预估时间**: 3-4 小时  
**依赖**: 无（可与 Task 6.1 并行）  
**状态**: ⏳ 待开始

**任务内容**:
- 调整 `NoteManager` 中的 `savedScrollTopRef` 使用策略：
  - 在移动端编辑过程中，不再在 `handleSegmentedEditorChange` 里频繁设置 `editorScrollContainer.scrollTop`。
  - 仅在视图切换回编辑页时，根据已有的 `savedScrollTopRef` 做一次性滚动恢复。
- 审视并精简 `SegmentedEditor` 内的滚动和光标恢复逻辑：
  - 在移动端保留“光标位置恢复”，但减少/取消对 textarea `scrollTop` 的主动干预。
  - 确保不会通过 `setInterval` 或多重 `requestAnimationFrame + setTimeout` 长时间反复调整滚动。

**技术实现要点**:
- 使用 `isMobile` 条件分支区分桌面端与移动端滚动恢复策略。
- 在 `useEffect`（视图切换）中限制恢复次数（如只恢复 1-2 次），避免“循环恢复”。
- 充分测试 iOS Safari / Android Chrome 的键盘弹出/收起场景，保证不出现新抖动。

**验收标准**:
- [ ] 移动端连续编辑同一篇长笔记 30 秒，页面不再出现“自动跳回顶部”现象。
- [ ] 切换到其他视图再返回编辑页时，可将内容滚回之前的大致位置。
- [ ] 桌面端编辑时的光标和滚动恢复体验保持不变或更好。

**相关文件**:
- `src/components/NoteManager.tsx`
- `src/components/SegmentedEditor.tsx`
- `docs/productmanager/移动端编辑体验优化_PRD.md`

---

### Task 6.4: 移动端 SegmentedEditor 内容布局微调
**优先级**: 🟡 P1（中优先级）  
**预估时间**: 3-4 小时  
**依赖**: Task 6.1（写作模式完成后更易调试）  
**状态**: ⏳ 待开始

**任务内容**:
- 针对移动端，微调 `SegmentedEditor` 的文本段布局：
  - 在小屏幕下适当减小单段 `min-height` 和段间 margin，避免一段内容占太多竖向空间。
  - 保持桌面端当前的舒适行高和间距不变。
- 评估是否需要在移动端提供“单段连续编辑模式”：
  - 用更接近单一 Textarea 的体验替代过于碎片化的多段视图（可以作为后续可选优化）。

**技术实现要点**:
- 在 `SegmentedEditor` 中通过媒体查询或运行时判断区分移动端样式：
  - 如在移动端将 `min-h-[120px]` 和 `margin-bottom: 1.5rem` 适当调低。
- 注意与浮动工具栏（Formatting Toolbar）和表格段的现有交互兼容。

**验收标准**:
- [ ] 移动端每段文本在视觉上更紧凑，但仍保持可读性。
- [ ] 桌面端段落间距和段高完全不受影响。
- [ ] 与现有浮动格式化工具栏、表格编辑等功能无冲突。

**相关文件**:
- `src/components/SegmentedEditor.tsx`
- `docs/productmanager/移动端编辑体验优化_PRD.md`

---

## 📋 Phase 7: 移动端六模式体验对齐（参考主流产品）

> 基于 `docs/features/优化思路.md` 中的六个关键模式拆解，对齐 iOS 备忘录 / 飞书 / 语雀 / Notion 等产品的移动端笔记体验。  
> 与 Phase 6 的写作模式、滚动修复等可并行或分阶段实施。

---

### 模式 1：单画布（One canvas）而不是两个框

#### Task 7.1.1: 移动端将标题作为画布首行而非独立输入框
**优先级**: 🟡 P1（高优先级）  
**预估时间**: 3-4 小时  
**依赖**: 无（可与 Task 6.x 并行）  
**状态**: ✅ 已完成

**任务内容**:
- 在移动端布局下，将标题从「独立 input/textarea 组件」改为与正文同一画布的首行。
- 画布内第一行即标题：同一可滚动区域内，标题与正文连续排布，无单独边框容器。
- 桌面端可保留现有标题+正文分栏/分块结构，通过媒体查询或布局分支区分。

**技术实现要点**:
- 在 `NoteEditor`/`NoteManager` 中根据 `isMobile` 或断点切换标题的渲染方式（独立框 vs 画布首行）。
- 画布首行方案下，标题与 `SegmentedEditor` 共用同一滚动容器，焦点与光标逻辑需兼容。

**验收标准**:
- [ ] 移动端进入笔记页时，标题与正文视觉上为同一块内容区域，无两个明显框框。
- [ ] 标题可正常聚焦、输入、保存。
- [ ] 桌面端行为与现有一致。

**相关文件**:
- `src/components/NoteEditor.tsx`
- `src/components/NoteManager.tsx`

---

#### Task 7.1.2: 用字号、字重、间距区分标题与正文（无边框）
**优先级**: 🟡 P1（高优先级）  
**预估时间**: 1-2 小时  
**依赖**: Task 7.1.1  
**状态**: ✅ 已完成  

**任务内容**:
- 标题与正文仅通过排版区分：标题字号更大、字重更重，与正文段间距加大。
- 移除标题与正文的边框、背景色等「表单感」样式，使整体像一张纸/一个画布。

**技术实现要点**:
- 使用 CSS 变量或 Tailwind 类控制标题行字体大小、font-weight、margin-bottom。
- 确保移动端与桌面端均可复用同一套排版 token。

**验收标准**:
- [ ] 无边框情况下，用户仍能清晰区分标题与正文。
- [ ] 视觉风格与「表单填写」区分明显，更接近阅读/写作场景。

**相关文件**:
- `src/components/NoteEditor.tsx`（或全局排版样式）
- 相关 CSS/ Tailwind 配置

---

#### Task 7.1.3: 进入页面默认阅读态，点击任意位置进入编辑
**优先级**: 🟢 P2（中优先级）  
**预估时间**: 2-3 小时  
**依赖**: Task 7.1.1  
**状态**: ✅ 已完成  

**任务内容**:
- 移动端进入笔记页时，默认以「阅读态」展示：无光标、无输入框边框，内容可滚动。
- 用户点击标题或正文区域后，再进入编辑态（聚焦、弹出键盘、显示工具栏等）。
- 降低一进页面就「被逼着填框」的压迫感。

**技术实现要点**:
- 新增「阅读态/编辑态」状态（如 `isReadingMode`），移动端默认 `true`。
- 点击内容区域时设为 `false` 并聚焦对应输入；顶栏「完成」或收起键盘时可恢复阅读态（可选）。

**验收标准**:
- [ ] 移动端首次进入笔记页，无光标、无明显输入框边界，可滚动浏览。
- [ ] 点击内容后正常进入编辑，键盘与工具栏行为符合预期。

**相关文件**:
- `src/components/NoteEditor.tsx`
- `src/components/NoteManager.tsx`

---

### 模式 2：顶部 App Bar 少而关键

#### Task 7.2.1: 统一移动端顶栏结构（左返回、中状态、右更多）
**优先级**: 🟡 P1（高优先级）  
**预估时间**: 2-3 小时  
**依赖**: 无  
**状态**: ✅ 已完成

**任务内容**:
- 移动端顶栏固定为：左侧返回、中间文档状态（可选）、右侧更多（⋯）。
- 返回按钮稳定、易单手触达；更多内收拢分享/导出/历史/删除/设置等，避免顶栏拥挤。

**技术实现要点**:
- 在 `NoteEditor` 或笔记页布局中抽离/统一顶栏组件，移动端专用结构。
- 与现有路由返回、保存状态等逻辑对接。

**验收标准**:
- [ ] 移动端顶栏仅保留返回、状态、更多三项主入口。
- [ ] 返回与更多在真机上触控区域足够（建议 ≥44px）。

**相关文件**:
- `src/components/NoteEditor.tsx` 或笔记页布局组件

---

#### Task 7.2.2: 顶栏中间文档状态展示（未保存/已保存/同步中）
**优先级**: 🟢 P2（中优先级）  
**预估时间**: 1-2 小时  
**依赖**: Task 7.2.1  
**状态**: ✅ 已完成  

**任务内容**:
- 在顶栏中间展示文档状态：未保存、已保存、同步中等，文案或图标简洁。
- 与现有保存/同步逻辑对接，不重复造轮子。

**验收标准**:
- [ ] 状态随保存/同步结果实时更新，不遮挡主要内容。

**相关文件**:
- 顶栏组件、`NoteManager` 保存状态

---

#### Task 7.2.3: 完成/退出编辑入口置于顶栏或键盘工具栏
**优先级**: 🟡 P1（高优先级）  
**预估时间**: 1 小时  
**依赖**: Task 7.2.1、Task 7.3.1  
**状态**: ✅ 已完成  

**任务内容**:
- 「完成」或「收起键盘/退出编辑」入口放在顶栏或键盘上方工具栏，避免页面中部单独大按钮。
- 用户可通过顶栏或键盘旁按钮明确退出编辑态。

**验收标准**:
- [ ] 移动端有明确且易发现的「完成」或「收起」入口，不依赖点击空白处猜测。

**相关文件**:
- 顶栏组件、键盘上方工具栏（Phase 7.3）

---

### 模式 3：键盘上方的编辑工具栏（Keyboard accessory）

#### Task 7.3.1: 实现键盘上方常驻编辑工具栏
**优先级**: 🔴 P0（必须）  
**预估时间**: 5-6 小时  
**依赖**: Task 6.1（写作模式）或现有移动端焦点检测  
**状态**: ✅ 已完成

**任务内容**:
- 移动端键盘弹出时，在键盘正上方增加一条编辑工具栏（Keyboard accessory）。
- 常驻按钮建议：撤销、重做、加粗、斜体、标题、列表/待办、引用、插入（图片/链接）。
- 工具栏随键盘一起出现/消失，不占用正文区域固定高度。

**技术实现要点**:
- 使用固定于视口底部的条带，或 `input`/`textarea` 的 `scrollIntoView` + 底部 padding，使工具栏始终在键盘上方。
- 与现有格式化命令（如 Markdown 插入）对接，可复用桌面端逻辑。

**验收标准**:
- [ ] 键盘弹出时，工具栏稳定出现在键盘上方，不被遮挡。
- [ ] 各按钮可正常触发格式化/插入，且不导致焦点丢失或页面跳动。

**相关文件**:
- `src/components/NoteEditor.tsx` 或新建 `MobileEditorToolbar.tsx`
- `src/components/NoteManager.tsx`

---

#### Task 7.3.2: 选中文本时工具栏切换为选区相关操作
**优先级**: 🟡 P1（高优先级）  
**预估时间**: 3-4 小时  
**依赖**: Task 7.3.1  
**状态**: ✅ 已完成  

**任务内容**:
- 当用户选中一段文字时，键盘上方工具栏内容切换为「与选区相关的操作」：如加粗、斜体、链接、引用等。
- 未选中时显示「插入块/段落类型」或默认操作集。

**技术实现要点**:
- 监听选区变化（`selectionchange` 或输入组件 onSelect），根据是否有选区切换工具栏配置或显示不同按钮组。

**验收标准**:
- [ ] 选中文字后，工具栏展示格式化类操作；取消选中后恢复为插入/段落类入口。

**相关文件**:
- 键盘上方工具栏组件、`SegmentedEditor` 或选区逻辑

---

#### Task 7.3.3: 未选中时提供插入块/快速结构入口（/ 或 +）
**优先级**: 🟢 P2（中优先级）  
**预估时间**: 3-4 小时  
**依赖**: Task 7.3.1  
**状态**: ✅ 已完成  

**任务内容**:
- 未选中文本时，工具栏提供「插入块」或「快速结构」入口：如输入 `/` 唤起块类型菜单，或「+」按钮展开列表/待办/引用/图片等。
- 参考 Notion/语雀的 `/` 命令或飞书的「+」插入。

**技术实现要点**:
- 可在工具栏放「+」按钮，点击弹出块类型列表；或监听输入 `/` 后弹出浮层菜单并插入对应 Markdown 或块结构。

**验收标准**:
- [ ] 用户可通过工具栏或 `/` 快速插入标题、列表、待办、引用、图片等，无需记忆语法。

**相关文件**:
- 键盘上方工具栏、块插入逻辑

---

### 模式 4：块/段落级交互

#### Task 7.4.1: 段落间距与块级结构样式（列表、待办、引用、分割线、代码块）
**优先级**: 🟡 P1（高优先级）  
**预估时间**: 3-4 小时  
**依赖**: 无  
**状态**: ✅ 已完成

**任务内容**:
- 统一段落之间间距，使块级结构更清晰。
- 为列表、待办、引用、分割线、代码块等提供一致且舒适的样式（移动端与桌面端均可复用）。
- 不强制引入完整 Notion 式块模型，先做「轻块」视觉与间距。

**技术实现要点**:
- 在 Markdown 渲染或编辑态下，为不同块类型增加 className 或 data 属性，统一 margin、padding、行高。

**验收标准**:
- [ ] 段落、列表、待办、引用、代码块在阅读与编辑态下均有清晰分隔，不糊成一团。

**相关文件**:
- `SegmentedEditor`、Markdown 渲染组件、全局排版样式

---

#### Task 7.4.2: 图片/附件作为独立块，支持 caption 与对齐
**优先级**: 🟢 P2（中优先级）  
**预估时间**: 2-3 小时  
**依赖**: Task 7.4.1（可选）  
**状态**: ✅ 已完成  

**任务内容**:
- 图片与附件在编辑/阅读时呈现为独立块，与前后段落间距明确。
- 支持可选 caption（说明文字）与对齐方式（左/中/右），提升「内容页」观感。

**验收标准**:
- [ ] 插入的图片在移动端以块形式展示，可带说明文字；对齐方式可选或默认居中。

**相关文件**:
- 图片/附件渲染、Markdown 或块解析

---

### 模式 5：阅读体验优先

#### Task 7.5.1: 统一排版系统（字体、行高、段距、最大宽度、留白）
**优先级**: 🟡 P1（高优先级）  
**预估时间**: 2-3 小时  
**依赖**: 无  
**状态**: ✅ 已完成

**任务内容**:
- 定义并落地一套排版 token：正文字体、行高、段间距、内容最大宽度、左右留白。
- 移动端与桌面端共用同一套变量，数值可按断点微调，使阅读更像「内容页」而非聊天框。

**技术实现要点**:
- 使用 CSS 变量或 Tailwind 配置集中管理 `font-size`、`line-height`、`margin-bottom`、`max-width`、`padding`。

**验收标准**:
- [ ] 笔记正文在移动端与桌面端阅读时，行宽与留白舒适，风格统一。

**相关文件**:
- 全局样式、`NoteEditor` / 阅读态样式

---

#### Task 7.5.2: 标题下元信息行（更新时间、标签等弱化展示）
**优先级**: 🟢 P2（中优先级）  
**预估时间**: 1-2 小时  
**依赖**: Task 7.1.2、Task 7.5.1  
**状态**: ✅ 已完成  

**任务内容**:
- 在标题下方增加一行弱化的元信息：如更新时间、标签、来源等（若存在）。
- 字体更小、颜色更浅，不抢夺正文注意力。

**验收标准**:
- [ ] 元信息行存在时清晰可读，但不喧宾夺主。

**相关文件**:
- `NoteEditor`、笔记头部布局

---

#### Task 7.5.3: 长文目录/大纲（H1/H2 跳转入口）
**优先级**: 🟢 P2（中优先级）  
**预估时间**: 3-4 小时  
**依赖**: Task 7.5.1  
**状态**: ✅ 已完成  

**任务内容**:
- 当笔记较长且包含标题结构（如 H1/H2）时，提供目录/大纲入口：点击可跳转到对应标题。
- 可放在顶栏更多内、或阅读态下浮动按钮，不强制常驻占位。

**技术实现要点**:
- 解析正文中的标题，生成锚点或 id；提供简单列表 UI 并 `scrollIntoView` 或路由 hash 跳转。

**验收标准**:
- [ ] 长文可通过目录快速定位到某一级标题。

**相关文件**:
- 笔记页、大纲组件

---

### 模式 6：减少边框，用状态表达可编辑

#### Task 7.6.1: 聚焦态/选区态样式（光标、微弱底色），移除默认大边框
**优先级**: 🟡 P1（高优先级）  
**预估时间**: 2-3 小时  
**依赖**: Task 7.1.2  
**状态**: ✅ 已完成  

**任务内容**:
- 去掉标题与正文的默认粗边框或明显框线。
- 仅通过「聚焦态」（如当前段落微弱底色或左边框）和「选区态」（选中高亮）表达可编辑，光标可见且稳定。

**技术实现要点**:
- 编辑态下为聚焦的段落或输入区域增加 `:focus` / `focus-within` 样式，移除 `border` 或改为极细/透明。

**验收标准**:
- [ ] 默认无大边框；聚焦/选中有清晰但克制的视觉反馈，移动端光标不丢失（可与 Task 6.3 一并验证）。

**相关文件**:
- `NoteEditor`、`SegmentedEditor`、焦点与选区样式

---

#### Task 7.6.2: Placeholder 引导语与首次进入轻提示
**优先级**: 🟢 P2（中优先级）  
**预估时间**: 1-2 小时  
**依赖**: Task 7.1.3  
**状态**: ✅ 已完成  

**任务内容**:
- 标题与正文的 placeholder 使用一句简短引导语（如「写个标题」「写正文…」），替代生硬的「请输入」。
- 可选：首次进入编辑态时，轻提示「向下输入正文 / 输入 / 插入内容」，一次即可。

**验收标准**:
- [ ] Placeholder 友好且不占太多视觉；若有首次提示，不打扰、易消失。

**相关文件**:
- `NoteEditor`、`SegmentedEditor`、i18n 或文案配置

---

## 📊 任务总览

### 任务统计
- **总任务数**: 30（含 Phase 7 共 17 个新 Task）
- **已完成**: 13
- **待开始**: 17（Task 6.1–6.4 + Phase 7 全部）
- **可选任务**: 2（Task 4.2, Task 4.3）

### 优先级分布
- **P0（必须）**: 6 个任务（含 Phase 6 与 Phase 7 的 7.3.1）
- **P1（高优先级）**: 8 个任务（含 Phase 1、Phase 6 的 P1 与 Phase 7 多项）
- **P2（中优先级）**: 8 个任务
- **P3（中优先级）**: 2 个任务
- **P4（低优先级）**: 3 个任务

### 预估总时间
- **必须任务（P0-P2）**: 约 25-32 小时（叠加移动端优化）
- **可选任务（P3-P4）**: 约 13-17 小时
- **总计**: 约 38-49 小时

### 依赖关系图
```
Task 0.1 (滑动误触修复)
  └─ 无依赖

Task 0.2 (拖拽冲突修复)
  └─ ✅ 已完成

Task 1.1 (统计信息组件)
  └─ 无依赖

Task 1.2 (集成统计信息)
  └─ Task 1.1

Task 2.1 (查找替换工具函数)
  └─ 无依赖

Task 2.2 (查找替换对话框)
  └─ Task 2.1

Task 2.3 (集成查找替换)
  └─ Task 2.2

Task 3.1 (搜索结果导航)
  └─ Task 2.1 (复用工具函数)

Task 3.2 (详情页结果切换)
  └─ Task 2.1 (复用工具函数)

Task 4.1 (基础表格插入)
  └─ 无依赖

Task 4.2 (表格编辑辅助)
  └─ Task 4.1 (可选)

Task 4.3 (可视化表格编辑器)
  └─ Task 4.1, Task 4.2 (可选)

Task 5.1 (笔记导出功能)
  └─ 无依赖
```

---

## 🚀 执行建议

### 第一阶段：Bug修复（必须）
1. Task 0.1 - 修复滑动误触问题
2. Task 0.2 - ✅ 已完成

### 第二阶段：快速提升（高优先级）
3. Task 1.1 - 创建统计信息组件
4. Task 1.2 - 集成统计信息

### 第三阶段：核心功能（中优先级）
5. Task 2.1 - 创建查找替换工具函数
6. Task 2.2 - 创建查找替换对话框
7. Task 2.3 - 集成查找替换

### 第四阶段：增强功能（中优先级）
8. Task 3.1 - 增强搜索结果导航
9. Task 3.2 - 实现详情页结果切换

### 第五阶段：扩展功能（低优先级，可选）
10. Task 4.1 - 实现基础表格插入
11. Task 4.2 - 实现表格编辑辅助（可选）
12. Task 4.3 - 实现可视化表格编辑器（可选，后续迭代）

### 第六阶段：导出功能（中优先级）
13. Task 5.1 - 实现笔记导出功能

### 第七阶段：移动端六模式体验对齐（参考主流产品）
14. Phase 7 - 单画布（Task 7.1.1–7.1.3）、顶栏（7.2.1–7.2.3）、键盘工具栏（7.3.1–7.3.3）、块级交互（7.4.1–7.4.2）、阅读体验（7.5.1–7.5.3）、状态表达（7.6.1–7.6.2）  
    - 详见 `docs/features/优化思路.md` 六模式索引与 Phase 7 各 Task 说明。

---

## 📝 注意事项

1. **代码复用**：Task 2.1 创建的查找工具函数可以被 Task 3.1 和 Task 3.2 复用
2. **移动端适配**：所有UI组件都需要考虑移动端适配
3. **性能优化**：使用 `useMemo` 和 `useCallback` 优化性能
4. **类型安全**：确保所有代码都有完整的 TypeScript 类型定义
5. **测试**：建议为关键功能编写单元测试
6. **Phase 7**：六模式任务与 Phase 6 移动端写作模式、滚动修复可并行或分阶段实施；实施顺序建议先 7.1/7.2/7.6（画布与顶栏与状态），再 7.3（键盘工具栏），最后 7.4/7.5（块级与阅读）

---

## ✅ 完成标准

每个任务完成后，需要：
1. 代码审查通过
2. 功能测试通过（包括移动端）
3. 更新任务状态为 ✅ 已完成
4. 更新相关文档（如有需要）
