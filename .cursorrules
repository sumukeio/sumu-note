# Sumu Note — Cursor 规则摘要

本文件为兼容入口。完整规范见根目录 **AGENTS.md**（最高仲裁）。

- 单任务推进 `taskNNN`，完成后必须刹车等待人类确认
- 禁止私自 git commit / push
- 文档驱动：`.phrase/` 管过程，`docs/` 管业务知识
- CHANGELOG 只追加：`docs/changelog/CHANGELOG.md`
- 技术栈：Next.js + Supabase + Vitest；密钥禁止入库
- 交互必须有可见反馈；核心逻辑修复需回归测试

详细规则目录：`.cursor/rules/`
