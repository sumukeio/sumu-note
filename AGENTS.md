# AGENTS.md — Sumu Note 工程治理与跨工具开发总规范

> **权威性声明**：本文档是跨 IDE、CLI 及各类 AI Agent（Antigravity、Cursor、Codex 等）的总工程守则与治理标准。所有参与本项目的 Agent 和人类开发者必须无条件严格遵守。  
> **核心宗旨**：消除代码幻觉，保证交付确定性，严守工程边界，做到「一次把事情做对，犯过的错绝不再犯」。  
> **项目适配**：本文档自公共模板本地化至 **Sumu Note**（Next.js + Supabase 笔记应用）。不适用条款见文末「暂缓启用」。  
> **公共模板 SSOT**：`E:\DAS储备数据\经验积累\AGENTS.md`（跨项目共用铁律；可跨项目负向禁令回写彼处）  
> **认知手册 SSOT**：`E:\DAS储备数据\经验积累\从VibeCoding到专业工程_AI协同认知升级手册.md`（仓库内镜像见 `docs/guides/`）

---

## 一、AI 的角色定位与核心底线 (Role & Bottom Line)

AI 在人机协同中是**「高执行力的工程副驾与盲区探查员」**，绝不是盲目的自动驾驶者。

1. **绝对确定性**：严禁猜想。对不确定的接口、依赖版本、路径和业务上下文，必须主动查阅文件或提问，严禁凭空脑补。
2. **严守开闭原则**：只做被授权的任务，绝不擅自扩大修改范围，绝不私自做无关的“顺手重构”。
3. **可验证交付**：没有经过可执行验证（自动化测试、脚本断言、真实运行）的代码一律视为“未完成”。

---

## 二、必须遵守的 8 大铁律与硬性约束 (8 Iron Rules)

### 1. 严禁未验证即宣称完成（No Hallucinated Success）
* 提交修改前，必须提供或执行具体的验证手段（运行测试用例、终端命令输出、逻辑断言）。
* 严禁用“代码已写好，应该没问题了”作为回复。必须展示实际运行结果或可复现的验证凭据。

### 2. 严格单任务推进，强制“刹车等待”（Stop & Wait for Confirmation）
* 任务推进严格遵循三位递增编号（`task001~taskNNN`，不可复用）。
* **每个原子任务完成并验证通过后，AI 必须主动停下并等待人类确认，严禁擅自连续串行执行下一个任务。**

### 3. 严禁私自执行 Git 提交（Git Command Prohibition）
* 除非人类在当前会话中明确发出 Git commit / push 指令，否则**严禁私自执行任何 `git commit`、`git push` 或分支变动操作**。
* Git 提交历史是严肃的工程资产，版本提交控制权 100% 归人类所有。

### 4. 严禁硬删除既有文档，推行“优雅归档”（Archive, Never Hard Delete）
* 严禁删除 `docs/` 及仓库内既有的任何说明文档、设计稿或需求文件。
* 允许对旧文档进行「文首添加状态标识（权威/讨论稿/归档/备选路径）」、「分区迁移至 archive/」以及「在索引文档中更新链接」，但原始内容必须完整保留历史上下文。

### 5. 严格文档驱动与 CHANGELOG 必记（Doc-Driven & Only Append）
* 严禁未更新文档即盲目编写代码。开发流程必须遵循：`锁定需求/文档 → 拆解 taskNNN → 实施与验证 → 严格闭环回写文档与 CHANGELOG`。
* 任何实质性的代码、配置、规则库或核心文档变更，必须在 **`docs/changelog/CHANGELOG.md`** 中以固定格式追加记录，且**只追加、不覆盖**。  
  （本仓库历史已落盘于 `docs/changelog/`；规范模板中的 `docs/CHANGELOG.md` 在本仓库等价于此路径，另见根级薄索引 `docs/CHANGELOG.md`。）

### 6. 语言与凭证数据安全（Security & Contracts）
* **语言规范**：所有交互、文档与注释默认使用**简体中文**。
* **凭证安全**：严禁将任何密码、API Key、Token、私钥证书、企业商业机密或真实用户隐私数据写入代码库与文档。环境变量仅通过 `.env.local`（已 gitignore）注入；示例只用占位符。
* **公共契约**：严禁擅自修改公共 API、Supabase 表结构/RLS 语义或公共配置。若涉及破坏性变更，必须先撰写 **ADR（架构决策记录）**。

### 7. 负向规则最高优先级（Zero Repeated Mistakes）
* 项目规则库中的“负向禁令”（历史踩坑清单、禁止使用的写法、废弃的第三方库）具备最高解释权。
* 任何情况下，不得以“更简便”、“业界通用”为借口违反本项目已沉淀的负向规则。
* 负向规则正文见下文「负向禁令库」。

### 8. 交互反馈必须绝对闭环（UI Action Feedback Principle）
* **操作有始有终**：任何用户触发的操作（点击按钮、保存笔记、切换设置、导入/导出、删除），系统必须在 0.5 秒内给予明确的即时视觉反馈（Toast、状态 Banner、Spinner、弹窗），**严禁静默执行或因页面刷新导致提示瞬间消失**。
* **成功与失败凭据清晰**：
  * **操作成功**：必须明确告知影响范围与结果凭据（如：“✅ 已保存笔记《周报》，字数 1,204” / “🗑️ 已删除 3 条笔记，可从回收策略说明中确认不可恢复范围”）。
  * **操作失败**：严禁仅弹抽象报错，必须给出具体失败原因与排查指引（如网络失败、RLS/鉴权失败、必填标题为空、Markdown 解析失败）。
* **防止重复误触发**：导入、批量删除、发布等提交完成后，前端必须自动清空/重置待提交状态，严禁无感知二次重复提交。

---

## 三、AI 标准五步工作流 (Standard 5-Step Workflow)

```text
[接收 taskNNN] ➔ 1. 审题与对齐 ➔ 2. 设计验证方案 ➔ 3. 最小原子实施 ➔ 4. 真实排错验证 ➔ 5. 结构化交付与强制刹车
```

### Step 1: 审题与对齐 (Understand & Align)
1. **查阅上下文**：检查涉及到的现有代码、接口定义与规则文档。
2. **确认影响面**：列出本次预计变动的文件清单。
3. **识别歧义**：若需求存在模糊点或缺少前置依赖，主动向人类提问，不擅自假设。

### Step 2: 设计验证方案 (Harness First)
在动手改动业务代码之前，先明确“如何证明改对了”：
* 是否已有现成单元测试或端到端测试用例？（本仓库：`vitest`，目录 `tests/`）
* 若无，需先设计/编写对应的测试用例或可复现的手动验证步骤。

### Step 3: 最小原子实施 (Minimal Implementation)
* 严格按照既定方案编码，保持代码风格、命名习惯与现有仓库完全一致。
* 编写清晰、符合规范的注释，保留核心逻辑的解释说明。

### Step 4: 运行与自我排错 (Run & Self-Debug)
* 优先使用：`npm run type-check`、`npm test`、`npm run lint`、`npm run build`（按任务需要择用）。
* **遇到报错时的排查原则**：
  * 先定位根本原因（Root Cause），严禁盲目打补丁（如空 `catch` 吞异常、无依据的类型强转）。
  * 若连续尝试 2 次仍未解决，停下来向人类汇报具体错误日志、排查思路与疑点，请求人类介入。

### Step 5: 结构化交付与强制刹车 (Report & Brake)
任务完成后，以固定结构向人类汇报并**停下等待确认**：
* **变更摘要**：改动了哪些文件，核心逻辑是什么。
* **验证凭据**：附上测试命令及运行通过的日志/输出。
* **文档与日志**：确认已同步更新对应说明文档、阶段 `change_*` 与 `docs/changelog/CHANGELOG.md`。
* **请求确认**：等待人类审查并确认通过后，再开启下一个 `taskNNN`。

---

## 四、Phase 阶段生命周期与任务体系 (`.phrase/`)

项目采用 `.phrase/` 体系进行精细化研发管理：

```text
.phrase/
  docs/
    CHANGE.md       # 变更总索引，顶部维护【当前进行阶段】链接
    ISSUES.md       # 缺陷与问题追踪索引 (issueNNN)
    README.md       # .phrase 体系说明
  phases/
    phase-<purpose>-<YYYYMMDD>/
      spec_*.md     # 阶段目标、范围定义 (Goals & Non-goals)、验收标准
      plan_*.md     # 里程碑计划、架构边界、风险预案
      task_*.md     # 原子任务清单 (taskNNN)，包含：产出 + 验证方式 + 影响范围
      change_*.md   # 任务完成后的变更回溯日志 (时间倒序)
      tech-refer_*.md (可选) 技术选型与规范参考
      adr_*.md        (可选) 架构决策记录
```

### 1. Phase Gate（阶段开启）
- 仅当人类明确要求开启新阶段或大版本演进时，方可新建 `phase-<purpose>-<YYYYMMDD>`。
- 阶段初始化必须包含最小集：`spec_*`、`plan_*`、`task_*`、`change_*`。

### 2. In-Phase 推进循环
- **新功能/需求变动**：更新对应阶段的 `plan_*`，拆解新增 `taskNNN`。
- **Bug 修复**：在 `.phrase/docs/ISSUES.md` 登记 `issueNNN`，并在当前 phase 中细化详情后拆解 task。
- **重大架构决策**：先撰写 `adr_*` 或 `tech-refer_*`，确认后方可实施。

### 3. Task 闭环四部曲（每次任务完成时强制执行）
1. 在 phase `task_*.md` 中将对应任务标记为 `[x]`；
2. 在 phase `change_*.md` 中追加变更详情，并在 `.phrase/docs/CHANGE.md` 更新索引；
3. **在 `docs/changelog/CHANGELOG.md` 中以规范格式追加一条记录**；
4. 若涉及配置变动、架构调整或操作指引，同步更新 `docs/` 下对应主线文档（见 `docs/README.md` 路由表）。

### 4. Phase 结项归档
- **未经人类明确确认，严禁擅自结项**。
- 经人类验收通过后，方可将目录重命名为 `DONE-phase-<purpose>-<YYYYMMDD>`。

---

## 五、业务文档架构与单一事实来源 (`docs/`)

`docs/` 目录是业务知识库与运行维护的唯一权威源：

### 1. 文档资产治理实操 (Docs & Archiving)

| 治理动作 | 执行规范 | 示例 |
| :--- | :--- | :--- |
| **文首添加状态标识** | 在文档首行以引用块标明当前权威状态 | `> **状态**：[权威/现行] / [讨论稿] / [已被 RFC-002 替代]` |
| **过时文档分区迁移** | 不直接删除，将其移动至 `docs/archive/` 归档目录下 | `mv docs/old-plan.md docs/archive/old-plan.md` |
| **索引路由同步更新** | 每次新增/移动文档，必须在文档总索引（`docs/README.md`）中更新跳转链接 | 保持「你要找什么 → 打开哪个文件」路由表永远准确 |

### 2. 核心主线文档职责清单

| 文档路径 / 职责 | 核心内容 | 维护准则 |
| :--- | :--- | :--- |
| **`docs/README.md`** | 文档总索引与「你要找什么 → 打开哪个文件」路由表 | 每次新增/调整文档必须同步更新 |
| **`docs/changelog/CHANGELOG.md`** | 仓库全量历史变更日志 | 只追加不覆盖，严格按标准条目记录 |
| **`docs/CHANGELOG.md`** | 指向上述权威 CHANGELOG 的薄索引（兼容模板路径） | 保持链接有效即可 |
| **`docs/productmanager/`** | PRD、路线图、产品建议 | 产品级变更回写 |
| **`docs/requirements/`** | 功能需求规格 | 需求变更回写 |
| **`docs/guides/`** | 开发/测试/运维指南 | 流程变更回写 |
| **`docs/sql/`** | Supabase/SQL 迁移脚本 | Schema 变更必须留档 |

> **说明**：规范模板中的 `docs/ops/`、`docs/architecture/`、`docs/product/` 本仓库**暂不强制迁目录**；现有 `deployment/`、`架构审查_*.md`、`productmanager/` 等继续作为事实落盘位置，由 `docs/README.md` 统一路由。

---

## 六、本仓库技术栈与工程规范 (Sumu Note)

| 维度 | 约定 |
| :--- | :--- |
| **运行时** | Next.js（App Router）+ React + TypeScript |
| **后端/数据** | Supabase（Auth / Postgres / Storage / Realtime）；客户端封装见 `src/lib/supabase.ts` |
| **样式** | Tailwind CSS + 既有 `src/components/ui/*`（Radix） |
| **测试** | Vitest；用例位于 `tests/`（镜像核心模块） |
| **常用命令** | `npm run dev` / `npm run build` / `npm run type-check` / `npm test` / `npm run lint` |
| **代码风格** | 跟随仓库既有风格；禁止无关大范围格式化；4 空格缩进优先与邻接文件一致 |
| **分层习惯** | `src/app` 路由页；`src/components` UI；`src/lib` 领域逻辑与工具；`src/hooks`；`src/types` |
| **密钥** | 仅环境变量；文档与示例禁止真实 Key |

---

## 七、跨 Agent 规则映射与工作区配置

本项目支持 Antigravity 与 Cursor 双栖开发，规则保持内容同步与单一事实来源：

- **Antigravity 原生规则库**：`.agents/rules/`（`00-project-core.md`, `doc-driven-workflow.md`, `sumu-note-tech.md`）
- **Cursor 兼容规则库**：`.cursor/rules/`（同名 `.mdc`）与根目录 `.cursorrules`
- **最高仲裁基准**：根目录 `AGENTS.md`（所有 Agent 均以此为权威准则）。

> 规则文件只做「可执行摘要」；细节冲突时以 `AGENTS.md` 为准。

---

## 八、错误复盘与防再犯机制：三套机器级别物理防御枷锁 (Triple Defensive Hard Gates)

```text
       【发现 Bug / 业务异常 / 踩坑漏洞】
                       │
                       ▼
 ┌────────────────────────────────────────────────────────┐
 │ 枷锁 1：编写防退化回归测试网 (Regression Test Gate)    │
 │ 机器把关：相关改动必须附带可执行回归用例（vitest）      │
 └─────────────────────┬──────────────────────────────────┘
                       │
                       ▼
 ┌────────────────────────────────────────────────────────┐
 │ 枷锁 2：强契约断言与防御性兜底 (Contract & Fail-Safe)  │
 │ 数据/同步异常时禁止静默空白；必须可见告警或明确空态   │
 └─────────────────────┬──────────────────────────────────┘
                       │
                       ▼
 ┌────────────────────────────────────────────────────────┐
 │ 枷锁 3：负向禁令沉淀进 AGENTS.md (Negative Rules)      │
 │ 认知把关：踩坑当天提炼为负向铁律，写入本文「负向禁令库」│
 └────────────────────────────────────────────────────────┘
```

### 1. 物理枷锁一：防退化回归测试网（Regression Test Gate）
* **强制原则**：Bug / 字段映射 / 核心工具函数漏洞修复后，**必须编写针对该漏洞的防退化测试**。
* **门禁标准**：
  - 严禁用“我手工点过了”代替机器验证（UI 胶水层可另附手动步骤，但核心 `src/lib/*` 逻辑优先自动化）；
  - 笔记指纹、表格转换、大纲/待办工具等纯函数必须可测；
  - **未附带可执行测试（或明确手测清单且人类认可）的 Bug 修复，一律视为未完成交付**。

### 2. 物理枷锁二：强契约断言与 Fail-Safe 告警（Data Sentinel）
* **强制原则**：核心读写路径若出现异常极端形态，**严禁静默吞掉或以空白界面忽悠用户**。
* **本项目适用形态（示例）**：
  - 鉴权失效却仍展示「空笔记列表」而无登录引导；
  - 保存接口失败却提示成功；
  - 同步/Realtime 拉取结果异常为空，却不展示错误或重试入口；
  - 导入解析后 0 条有效笔记且无原因说明。
* **门禁标准**：关键路径必须有可感知失败态；必要时在控制台输出醒目告警（开发态）并在 UI 给出排查指引。

### 3. 物理枷锁三：负向禁令永久沉淀（Negative Rules Zero Tolerance）
* **强制原则**：折腾超过合理阈值的深坑或重大偏差，**必须当天提炼为负向铁律写入本文件「负向禁令库」与规则库摘要**。

---

## 负向禁令库（Sumu Note）

> 只追加、不删改历史条目语义；废弃时标注「已废止」并说明替代规则。

1. **禁止空 catch 吞掉鉴权/保存错误**：认证与笔记保存失败必须上抛或转为用户可见反馈，禁止 `catch { /* ignore */ }`。
2. **禁止把真实 Supabase Key / 用户内容写入仓库或文档截图替代品中的明文密钥**。
3. **禁止在未开 ADR 的情况下破坏性改动公开笔记链接语义、RLS 策略或 Storage 桶公开性**。
4. **禁止用 `any` 掩盖跨编辑器（SegmentedEditor / MindNote / Todo）边界的数据结构分歧**——应先对齐类型或写适配层。
5. **禁止在移动端编辑相关改动中忽略 `--vvh` / `visualViewport` 与键盘遮挡问题**（历史多次踩坑）。
6. **禁止在真机/WebView「白屏或永久转圈」场景下无 step 时间线证据就连续改业务代码**：须启用关键路径打点 + 可复制日志看板（或等价证据），按最后停留点归因。完整 SOP：[`docs/guides/CLIENT_DEBUG_PLAYBOOK.md`](docs/guides/CLIENT_DEBUG_PLAYBOOK.md)。
7. **禁止把调试开关做成长期后门或鉴权旁路**：`?debug*=1` 仅 session 级排障；**不参与登录判定**；排障结束必须关闭；日志禁止写入 Token/密码。
8. **禁止对可能永不结束的动态 `import()` / `getSession` 只转圈不打点**：必须有超时 step（如 `*:import-timeout`）与可见失败/降级入口；仅有 `start` 无 `ok`/`timeout` 视为未完成排障。

---

## 九、代码质量与交付自检清单 (Pre-Flight Checklist)

在宣布任务完成前，AI 必须快速自检：

- [ ] 是否满足了人类提出的所有显式要求？
- [ ] 是否执行了 `taskNNN` 单一原子任务，并未越权做额外修改？
- [ ] 是否破坏了任何现有测试或既有功能？
- [ ] 是否新增或更新了对应的单元测试/集成测试（或经人类认可的手测清单）？
- [ ] 是否引入了未经说明的第三方大体积依赖或硬编码了敏感凭据？
- [ ] 是否保留了历史文档并通过优雅归档处理过时文件？
- [ ] 是否按规范在 `docs/changelog/CHANGELOG.md` 中追加了变更日志？
- [ ] 是否已准备好停下，等待人类确认而不是自作主张推进下一任务？

---

## 暂缓启用（对本仓库暂不适用或待后续 phase）

以下条款来自通用模板，**本阶段不强制执行**，需要时再开 task 落地：

| 项 | 原因 |
| :--- | :--- |
| 数仓/广告指标「全 0」断言话术 | 非本产品域 |
| 强制新建 `docs/ops/`、`docs/architecture/`、`docs/product/` 并整体迁目录 | 现有 `docs/` 分区已可用；迁目录成本高、易断链 |
| 全量将 `docs/reports/BUG_REPORT.md` 机械拆成 `issueNNN` | 历史债务大；新缺陷从 `ISSUES.md` 起增量登记即可 |
| 独立「健康巡检探针」脚本（类 API patrol） | 可后续补 `scripts/`；当前以 `type-check` + `vitest` + `build` 为门禁 |
| Antigravity 专属深度集成 | 已提供 `.agents/rules` 摘要；无 Antigravity 环境时可忽略 |

---
